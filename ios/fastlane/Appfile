app_identifier("com.demo.fastlaneDemo")
apple_id(ENV["APPLE_ID"] || "your-apple-id@example.com")
team_id(ENV["APPLE_TEAM_ID"] || "YOUR_TEAM_ID")

# App Store Connect API key (recommended for CI)
# Set APP_STORE_CONNECT_API_KEY_PATH or use match / Apple ID password in CI secrets
